-- The ai.copilot extra itself is imported in lua/config/lazy.lua (LazyVim
-- extras must be top-level imports, not nested in lua/plugins/*.lua files).
-- Run :Copilot auth after install/restart to authorize.
return {}
